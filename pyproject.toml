[project]
name = "star-polymarket"
version = "0.1.0"
description = "Star Polymarket - Polymarket API integration"
requires-python = ">=3.12"
dependencies = [
    "polymarket-apis>=0.1.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.4.0",
]
